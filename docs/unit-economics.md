# БАЗА — Unit-экономика (измерено + сведено под ×10, 2026-06-29)

> req/лид **измерен напрямую** на живом ключе (0,214). Ставка Яндекса «с
> сохранением» уточнена у поддержки: ≤30 дней разрешено на обычном тарифе →
> опубликованные ставки и есть реальные. Тарифы сведены под **гарантированную
> наценку ×10** смешанным рычагом (рез лидов + рост цен) + пер-орг кап Яндекс-
> запросов как страховка. Финал цен закрыт (п.4, п.6).

## 1. Измеренные константы (14 ниш × гео, 141 запрос, реальный ключ)

| Метрика | Значение |
|---|---|
| **Организаций на полный запрос** | median **20** (= параметр `results=20`), mean полных страниц 18,9 |
| **req/лид (взвешенный)** | **0,214** |
| req/лид — плотные ниши | 0,10–0,25 |
| req/лид — узкие ниши | 0,31–0,52 (median 0,191, p90 **0,41**, max 0,52) |
| **Доля с телефоном** | **96 %** |
| **Доля с сайтом** | **79 %** |
| Глубина одного запроса | ≤ **126 орг** (API режет доступ) → объём набирается числом query-термов |

Замер воспроизводим: `scripts/yandex_cost_measure.py` (нужен
`YANDEX_MAPS_API_KEY` в env; эндпоинт `search-maps.yandex.ru/v1/`,
фильтр `_candidate_relevance_score ≥ 26`).

## 2. Тарифы Яндекс Geosearch (опубликованные = «без сохранения»)

| Тариф | Цена/мес | Ёмкость/мес | ₽/запрос (эффект.) | Превышение |
|---|---|---|---|---|
| 1 000 запр./сутки | 20 800 ₽ | 30 000 | **0,69 ₽** | 390 ₽/1000 (0,39) |
| 10 000 запр./сутки | 58 500 ₽ | 300 000 | **0,195 ₽** | 195 ₽/1000 (0,195) |

✅ **«С сохранением» РАЗРЕШЕНО уточнено (поддержка Яндекса, 2026-06-29).** Дословно:
«Мы допускаем сохранение и хранение результатов работы API на срок **до 30 дней**
и без расширенного тарифа». То есть для нашего кейса опубликованные ставки выше —
**это и есть наши реальные ставки**, никакого множителя ×2–5. Опция «Сохранение
результатов» (бессрочно, >30 дней) есть только на **годовом** тарифе; на месячных
её нет и она не нужна. Единственное следствие для продукта: сырые Яндекс-данные в
**складе** держать ≤30 дней (TTL) — доставленный клиенту лид это уже его данные.

## 3. Калькулятор себестоимости (живая формула)

```
Яндекс_кост(тариф) = квота_лидов × req_на_лид × ставка_запроса
                   = квота_лидов × 0,214 × ставка
себестоимость      = Яндекс_кост + LLM (≈ квота×0,01 ₽) + эквайринг (3 % цены)
наценка            = цена / себестоимость
```

`ставка`: 0,69 (1k/сут) · 0,195 (10k/сут). Множителя «с сохранением» нет (п.2).
Starter: Яндекс_кост = 0 (источник 2ГИС/SearXNG, `use_yandex` только Pro/Team).

## 4. Тарифы под ×10 (2026-06-29) — гарантированная наценка

Цель: **наценка ×10** (себестоимость ≤ 10 % цены) даже в худшем случае — когда
выбран и Яндекс-кап, и AI-кап, плюс эквайринг 3 %. Рычаг — **смешанный**:
умеренный рез лидов + умеренный рост цен. Считается по реальной ставке 0,69 ₽
(1k/сут); на 10k/сут наценка ещё выше.

| Тариф | Цена | Лидов | Yandex-кап | AI-кап | Себест. (худшая) | Наценка |
|---|---|---|---|---|---|---|
| Starter | 3 900 | 5 000 | 0 | 100 ₽ | ~217 ₽ | **×18** |
| Pro | **16 900** | **7 000** | **1 400** | 200 ₽ | ~1 673 ₽ | **×10,1** ✅ |
| Business | **44 900** | **20 000** | **3 800** | 500 ₽ | ~4 469 ₽ | **×10,0** ✅ |

Себест. = Yandex-кап × 0,69 + AI-кап + эквайринг 3 %. Pro: 1 400×0,69 + 200 + 507
= 1 673 → ×10,1. Business: 3 800×0,69 + 500 + 1 347 = 4 469 → ×10,0. Реальная
наценка выше — фактический расход Яндекса/AI обычно ниже капов (типовой AI
~₽50/80/200). История цен/квот: 25k/100k → 10k/30k (26.06) → 7k/20k + рост цен
(29.06). Существующие пилоты grandfathered до смены тарифа.

## 5. Пер-орг кап Яндекс-запросов (внедрено — страховка от убытка)

Метеринг по ФАКТУ: каждый платный org-search запрос (включая geo/bbox-lookup)
пишется в `organizations.yandex_requests_used_current_month`; сброс 1-го числа
вместе с лидами/AI. Гейт в `collect_leads_task`:
`use_yandex = plan ∈ {pro,team} И yandex_requests_remaining(org) > 0` — при
исчерпании добор молча идёт с бесплатных источников (2ГИС/SearXNG).

Капы (`quota.PLAN_LIMITS["yandex_requests"]`) — бюджет ограничен сверху при ЛЮБОЙ
цене Яндекса:

| Тариф | Кап запросов/мес | Худший кост @1k/сут (0,69 ₽) | ≈ Yandex-лидов (0,214) |
|---|---|---|---|
| Free / Starter | 0 | 0 | — (только 2ГИС/SearXNG) |
| Pro | 1 400 | ~966 ₽ | ~6 500 (квота лидов 7k бьёт почти вровень) |
| Business (team) | 3 800 | ~2 622 ₽ | ~17 750 (квота лидов 20k чуть выше) |

Капы — конфиг-кнобы под целевую наценку ×10. Поднимать при переходе на дешёвый
10k/сут тариф. Реализация: `entities.py` (2 колонки) ·
`quota.py` (PLAN_LIMITS + `yandex_requests_remaining`) · `lead_collection.py`
(`_charge_yandex_requests`, счётчик в `_search_yandex_maps`) · `jobs.py` (гейт) ·
`periodic.py` (сброс) · миграция `d7b3e4c81f92`. Точность метеринга проверена
живьём: счётчик == фактические запросы (8 == 8).

## 6. Статус цен — ЗАКРЫТО (×10)
1. ✅ Ставка «с сохранением» уточнена — ≤30 дней на обычном тарифе, множителя нет (п.2).
2. ✅ Тарифы сведены под строгий ×10: Starter 3 900/5k · Pro 16 900/7k · Business 44 900/20k,
   капы Яндекс 0/1 400/3 800, AI 100/200/500 ₽ (`quota.py` + `plans.py`).
3. ✅ **Сделано (2026-07-03):** TTL ≤30 дней на Яндекс-данные в складе `companies`.
   Ежедневная задача `periodic.purge_yandex_warehouse_ttl` (beat 04:30 UTC): строки с
   `'yandex_maps' in sources`, не виденные > `yandex_raw_ttl_days` (30) дней — Яндекс-only
   удаляются, мульти-источниковым снимается метка `yandex_maps`. `raw_json` всегда пуст;
   доставленные лиды (`leads`) не трогаются. Тест `test_yandex_warehouse_ttl.py`.
4. Тариф API повышать только по breakeven (10k/сут оправдан от ~127k запр./мес).

## 7. Фиксированные расходы (не на клиента — степ-фикс)
2ГИС ~6 700 ₽/мес (→ 13 400 на масштабе) · VPS ~2 000 ₽ (→ 6 000) ·
Яндекс-подписка 20 800 ₽ (1k/сут) подключается с появлением платящих Pro.
На низком масштабе именно фикс делает каждого клиента (включая Starter) дорогим:
8 700 ₽ ÷ 5 клиентов = 1 740 ₽/клиента.
